# Cat

`cat` resolves cached or transformed content for one target and returns materialized text.

## Rules

- Content lookup is config-aware.
- Referenced local artifacts must point to stable cache-owned paths.
