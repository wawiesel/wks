# Log

The log layer exposes configured retention and status information for WKS logs.

## Rules

- Informational/debug output belongs in logs, not CLI STDOUT.
- CLI writes warnings/errors to STDERR.
