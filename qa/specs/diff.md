# Diff

The diff layer compares two inputs through a selected engine and returns one consistent structured result.

## Rules

- Engines are selected explicitly.
- Output shape stays stable for the same command.
