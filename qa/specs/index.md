# Index

The index layer stores searchable chunks and optional embeddings for named indexes.

## Rules

- Index behavior is config-driven.
- Embedding-backed indexes are explicit.
- Search and similar features consume the same index state.
