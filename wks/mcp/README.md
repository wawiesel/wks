# MCP Layer

Defines shared MCP response types and helpers. The MCP server dispatches tools declared in `wks/mcp_server.py` and delegates all work to controllers. Keep this layer protocol-only—no business logic or config defaults here.
