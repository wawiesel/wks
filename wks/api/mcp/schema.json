{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wks/specifications/mcp_output.schema.json",
  "title": "WKS MCP Output Schemas",
  "type": "object",
  "definitions": {
    "Base": {
      "type": "object",
      "properties": {
        "errors": { "type": "array", "items": { "type": "string" } },
        "warnings": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["errors", "warnings"],
      "additionalProperties": true
    },
    "McpListOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "targets": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "install_command": { "type": "string" },
                  "uninstall_command": { "type": "string" }
                },
                "required": ["name", "description", "install_command", "uninstall_command"],
                "additionalProperties": false
              }
            },
            "count": { "type": "integer" }
          },
          "required": ["targets", "count"],
          "additionalProperties": false
        }
      ]
    },
    "McpInstallOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "success": { "type": "boolean" },
            "name": { "type": "string" },
            "command": { "type": "string" }
          },
          "required": ["success", "name", "command"],
          "additionalProperties": false
        }
      ]
    },
    "McpUninstallOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "success": { "type": "boolean" },
            "name": { "type": "string" },
            "command": { "type": "string" }
          },
          "required": ["success", "name", "command"],
          "additionalProperties": false
        }
      ]
    }
  },
  "oneOf": [
    { "$ref": "#/definitions/McpListOutput" },
    { "$ref": "#/definitions/McpInstallOutput" },
    { "$ref": "#/definitions/McpUninstallOutput" }
  ]
}
