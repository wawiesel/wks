# Display Layer

Formats controller data for CLI and MCP views. Use these helpers to keep STDOUT for results and STDERR for progress/errors. Add new renderers here instead of formatting inside CLI or controllers.
