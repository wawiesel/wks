{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wks/specifications/index_output.schema.json",
  "title": "WKS Index Output Schemas",
  "type": "object",
  "definitions": {
    "Base": {
      "type": "object",
      "properties": {
        "errors": {
          "type": "array",
          "items": { "type": "string" }
        },
        "warnings": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["errors", "warnings"],
      "additionalProperties": true
    },
    "IndexOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "index_name": { "type": "string" },
            "uri": { "type": "string" },
            "chunk_count": { "type": "integer" },
            "checksum": { "type": "string" }
          },
          "required": ["index_name", "uri", "chunk_count", "checksum"],
          "additionalProperties": false
        }
      ]
    },
    "IndexStatusOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "indexes": {
              "type": "object",
              "additionalProperties": {
                "type": "object",
                "properties": {
                  "document_count": { "type": "integer" },
                  "chunk_count": { "type": "integer" },
                  "uris": {
                    "type": "array",
                    "items": { "type": "string" }
                  }
                },
                "required": ["document_count", "chunk_count", "uris"]
              }
            }
          },
          "required": ["indexes"],
          "additionalProperties": false
        }
      ]
    },
    "IndexAutoOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "uri": { "type": "string" },
            "priority": { "type": "number" },
            "indexed": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "index_name": { "type": "string" },
                  "chunk_count": { "type": "integer" },
                  "checksum": { "type": "string" }
                },
                "required": ["index_name", "chunk_count", "checksum"]
              }
            },
            "skipped": {
              "type": "array",
              "items": { "type": "string" }
            }
          },
          "required": ["uri", "priority", "indexed", "skipped"],
          "additionalProperties": false
        }
      ]
    },
    "IndexEmbedOutput": {
      "allOf": [
        { "$ref": "#/definitions/Base" },
        {
          "type": "object",
          "properties": {
            "index_name": { "type": "string" },
            "embedding_model": { "type": "string" },
            "chunk_count": { "type": "integer" },
            "dimensions": { "type": "integer" }
          },
          "required": ["index_name", "embedding_model", "chunk_count", "dimensions"],
          "additionalProperties": false
        }
      ]
    }
  },
  "oneOf": [
    { "$ref": "#/definitions/IndexOutput" },
    { "$ref": "#/definitions/IndexStatusOutput" },
    { "$ref": "#/definitions/IndexAutoOutput" },
    { "$ref": "#/definitions/IndexEmbedOutput" }
  ]
}
